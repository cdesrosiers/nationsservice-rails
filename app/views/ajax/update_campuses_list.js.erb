<% if @campuses.any? %>
$('#campus_select').html('<option>Select an Institution</option><%= escape_javascript(options_for_select(@campuses.map {|cam| [cam.name, cam.id]})) %>');
$('#campus_select').removeAttr('disabled');
<% end %>