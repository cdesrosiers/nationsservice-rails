$('#campus_select').remove();
<% if @campuses.any? %>
$('#edit-institution').append('<%= escape_javascript(collection_select :user, :campus_id, @campuses, :id, :name, prompt: 'Select a Campus', id: "campus_select") %>');
<% end %>