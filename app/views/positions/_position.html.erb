<tr>
  <td>
  </td>
  <td>
    <%= link_to position.name, position %>
  </td>
  <td>
    <ul class="locale-list">
      <% position.locations.each do |location| %>
        <li><%= location_name_short(location) %></li>
      <% end %>
    </ul>
  </td>
  <td>
    <% if !position.deadline.nil? %>
      <%= position.deadline.strftime('%m/%d/%y') %>
    <% end %>
  </td>
  <td>
    <% if !position.description.nil? %>
      <%= position.description %></br>
    <% end %>
  </td>
  <td>
    <% if !position.duration.nil? %>
      <%= position_duration(position) %></br>
    <% end %>
  </td>
  <td>
  </td>
  <td>
    <% if !position.poster.nil? %>
      <%= link_to position.poster.name, position.poster %></br>
    <% end %>
  </td>
</tr>
