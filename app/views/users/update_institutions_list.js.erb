$('#user_institution_id').html('<option value="">Select an Institution</option><%= escape_javascript(options_for_select(@institutions.map {|ins| [ins.name, ins.id]})) %>');
$('#user_campus_id').remove();